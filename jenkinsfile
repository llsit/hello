pipeline {
  agent any
  stages {
    stage('build') {
      steps {
        bat 'start cmd.exe /c {$PATH} build'
      }
    }

  }
   environment {
      PATH = "C:\\Ruby24-x64\\bin\\fastlane"
    }
}